Title: Built an AI Resume Screening System with LangChain + LangSmith

Post draft:

I built a modular AI Resume Screening System using LangChain and LangSmith for tracing. The pipeline performs skill extraction, matching, scoring, and generates human-readable explanations. I used structured prompts and deterministic fallbacks so the system runs even without an LLM key.

Repo: <your-github-repo-url>

Highlights:
- Modular `prompts/` and `chains/` structure
- LangSmith tracing for end-to-end observability
- Scoring + explainability for hiring decisions

Screenshot: (attach LangSmith trace screenshot)

If you'd like, I can walk you through the code or demo the pipeline — DM me!
